<div class="row row-create">
    <div class="col-md-3">
        <a href="{{ route($route) }}" class="btn btn-block btn-{{ $color or 'primary' }}" title="{{ $label }}">{{ $label }}</a>
    </div>
</div>